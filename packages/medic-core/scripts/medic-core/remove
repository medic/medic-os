#!/bin/sh

remove()
{
  local self="`realpath "$0"`"
  local base="`dirname "$self"`"

  source "$base/env" &&
  merge_environment /srv

  rm -rf "$PACKAGE_SOFTWARE" "$PACKAGE_SCRIPTS" "$PACKAGE_SETTINGS" \
    "$PACKAGE_STORAGE"/*/logs "$PACKAGE_STORAGE"/*/state
}

remove "$@"
exit "$?"

