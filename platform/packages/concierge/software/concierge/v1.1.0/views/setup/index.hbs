
<form id="form" method="post" action="/setup/password">

<div class="logo-wrapper">
  <div class="logo">
  </div>
</div>

<div class="body">
  <div class="content">
    <div class="steps">
      <div class="step {{#cmp data.step '>' 1}}finished{{/cmp}}">
        <div class="number">
          <span class="before">1</span>
          <span class="after fa fa-check"></span>
        </div>
        <label>Set up an administrator account</label>
      </div>
      <div class="step {{#cmp data.step '>' 2}}finished{{/cmp}}">
        <div class="number">
          <span>2</span>
        </div>
        <label>Start up required services</label>
      </div>
    </div>

    {{#cmp data.step '<=' 1}}
    <div class="inner">
      <p>
        Welcome to Medic Mobile! To get started, we'll need to create
        an administrator account for you.
      </p>
      <p>
        You'll use this account to log in and configure the system
        once it's up and running. Please choose a password that is
        eight characters or longer, known only to you, and easy for
        you to remember without writing it down.
      </p>
      <div class="rows">
        <div class="row">
          <label for="password">User ID</label>
          <div class="field">
            <input type="text" disabled="disabled" value="admin" />
          </div>
        </div>
        <div class="row">
          <label for="password">Password</label>
          <div class="field">
            <input class="full"
              type="password" id="password" name="password" />
          </div>
        </div>
        <div class="row">
          <label for="confirmation">Repeat Password</label>
          <div class="field">
            <input class="full" type="password"
              id="confirmation" name="confirmation" />
          </div>
        </div>
      </div>
      <p>
        A <em>public key</em> lets you share administrator access
        with someone else. If you have a public key or Medic Mobile
        has provided one to you, please enter it here. Otherwise,
        leave this blank and only you will have administrative access.
      </p>
      <div class="rows">
        <div class="row tall">
          <label for="key">Public Key(s)</label>
          <div class="hint"><b>Optional field</b></div>
          <div class="hint">OpenSSH format only</div>
          <div class="field">
            <textarea id="key"
              name="key" spellcheck="false">{{data.key}}</textarea>
          </div>
        </div>
      </div>
    </div>
    {{/cmp}}

    {{#cmp data.step '===' 2}}
    <div class="inner">
    </div>
    {{/cmp}}
  </div>

  <div class="actions">
    <div class="status">
      {{#if messages.error}}
        <span class="error">
          {{messages.error}}.
        </span>
      {{else}}
        {{#if messages.success}}
          <span class="success">
            {{messages.success}}.
          </span>
        {{else}}
          Please select an administrative password
          for your new virtual server.
        {{/if}}
      {{/if}}
    </div>
    {{#cmp data.step '>=' 2}}
      <button id="finish" class="button">
        <label>Finish</label>
      </button>
    {{else}}
      <button id="next" class="right">
        <label>Next Step</label>
        <span class="icon fa fa-chevron-right"></span>
      </button>
    {{/cmp}}
    <button id="back" {{#cmp data.step '<=' 1}}disabled="disabled{{/cmp}}">
      <span class="icon fa fa-chevron-left"></span>
      <label>Previous Step</label>
    </button>
  </div>

</div>

</form>

