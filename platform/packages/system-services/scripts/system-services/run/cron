#!/bin/sh

start()
{
  local logs_dir='/srv/storage/system-services/logs'
  local log_path="$logs_dir/cron.log"

  mkdir -p "$logs_dir" &&
  \
  exec /boot/timestamp crond -f -L "$log_path" \
    >> startup.log 2>&1
}

main()
{
  start
  return "$?"
}

main
exit "$?"

